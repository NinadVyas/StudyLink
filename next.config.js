/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','brighterion.com', 'www.smartdatacollective.com' ],
      },
}

module.exports = nextConfig
